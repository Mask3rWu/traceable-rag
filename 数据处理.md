# 毁伤评估资料结构化处理方案

## 1. 资料现状

当前资料共 47 份 PDF：

- 国军标 17 份、421 页，其中 15 份为纯图片 PDF，395 页没有文本层；
- 论文 30 份、621 页，大部分具有可用文本层；
- 部分页面同时包含水印、双栏排版、图表、公式和低清晰度图片。

处理目标不是简单生成 Markdown，而是建立能够回溯到原始文件、页码和区域的结构化证据库。

## 2. 数据分层

```text
资料/                         原始文件，永不修改
processed/parsed/             OCR、版面和表格等机器提取结果
processed/curated/            人工确认后的证据和评估准则
```

JSON/JSONL 作为程序处理的主格式，Markdown 仅用于人工查看和审核。

核心数据关系：

```text
document -> page -> block -> evidence -> rule
```

内容块类型包括：`heading`、`paragraph`、`list`、`table`、`formula`、`figure`、`caption`、`appendix`。

每个文档、页面和内容块都要有稳定编号。

```json
{
  "block_id": "GJB_001_P017_B03",
  "document_id": "GJB_001",
  "page": 17,
  "section_path": ["附录C", "C.4"],
  "block_type": "table",
  "bbox": [0.12, 0.18, 0.91, 0.82],
  "text": "识别出的内容",
  "source_method": "ocr",
  "confidence": 0.86,
  "is_appendix": true,
  "appendix_type": "资料性附录",
  "image_crop": "assets/GJB_001_P017_B03.png"
}
```

## 3. 总体处理流程

```text
文件清点与哈希
    -> 逐页判断文本层和版面类型
    -> 原生文本提取或扫描页 OCR
    -> 标题、正文、表格、公式、图片和附录识别
    -> 恢复章节关系与阅读顺序
    -> 质量检测和人工复核
    -> 按问题矩阵抽取证据
```

必须按页分流，不能简单地按整份 PDF 决定是否 OCR。一份文件中可能同时存在原生文本页、扫描页和图片页。

## 4. 工具建议

- `PyMuPDF`：PDF 检查、页面渲染、坐标和原生文本提取；
- `PaddleOCR/PP-Structure`：中文 OCR、版面和表格识别；
- `MinerU`：论文版面、公式、图表和双栏解析；
- `OpenCV`：去噪、纠偏、水印弱化和表格线处理。

国军标资料应优先在本地处理，不上传公共云端服务。

## 5. 特殊问题处理

### 5.1 水印

原始页面必须保留，另生成用于 OCR 的预处理版本。

- 独立水印图层：提取时忽略对应对象；
- 彩色浅水印：通过颜色空间分离生成弱化版本；
- 扫描件内嵌水印：根据跨页固定位置和颜色检测；
- OCR 后重复出现的水印文字：按位置和跨页频率过滤。

建议同时对原图和弱化水印图 OCR，选择质量更高的结果。不要对全部页面直接强二值化，以免损坏细小文字和表格线。

### 5.2 正文与附录引用

附录属于同一文档的结构节点，不应被切成无关文本。需记录：

- 附录编号和标题；
- `规范性附录`、`资料性附录`等性质；
- 正文条款与附录条款的引用关系。

```json
{
  "source_block": "GJB_001_5.3.2",
  "relation": "references",
  "target_block": "GJB_001_APPENDIX_A"
}
```

检索到“见附录A”的正文时，应展开附录内容；检索到附录内容时，也应返回引用它的正文条款。

### 5.3 模糊图片

处理顺序：

```text
400～600 DPI 渲染
-> 单图裁剪
-> 灰度、对比度增强、纠偏和适度锐化
-> OCR
-> 低置信度标记
-> 人工复核
```

无法辨认的文字标记为 `unreadable`，不得让模型根据上下文补写。模型生成的图片说明只能标记为推导信息，不能代替原始证据。

### 5.4 表格、图表和公式

表格需同时保存原始截图、标题、行列结构、单元格文本、合并单元格、单位和脚注。

公式需保存原始截图、LaTeX 或其他结构化表达、公式编号、变量解释及前后说明段落。

图表需保存图片、标题、页码、区域坐标和附近正文。对于毁伤准则，优先保证等级、阈值、注释和脚注的准确性。

### 5.5 双栏论文

不能直接依赖 PDF 返回的文本顺序。应通过内容块坐标恢复：

```text
跨栏标题
-> 左栏从上到下
-> 右栏从上到下
-> 跨栏图表及标题
```

需要识别跨栏图片、表格和公式，避免左右栏内容错误交叉。

## 6. 切块与证据抽取

版面和章节恢复后再切块：

- 国军标按条款编号、附录条目切分；
- 论文按章节和自然段切分；
- 表格与标题、脚注保持关联；
- 公式与变量解释保持关联；
- 正文对附录的引用不能断开；
- 固定长度只用于控制片段大小，不能替代结构切块。

每条证据至少记录：规则编号、目标类型、部件、可见特征、等级关系、原文、文件名、章节、页码、推导类型和置信度。

## 7. 试点与质量验收

不要立即处理全部资料。先选择以下代表性页面：

- 扫描国军标正文；
- 密集表格附录；
- 带文本层和水印的标准；
- 双栏、公式和水印并存的论文；
- 长篇学位论文；
- 英文论文。

每类先处理 5～10 页，检查：

- OCR 文字准确度；
- 双栏阅读顺序；
- 表格单元格完整度；
- 附录引用关系；
- 页码和区域可回溯性；
- 低质量内容是否被正确标记。

试点稳定后再进行全量解析。第一阶段的实现目标是完成文件清单、逐页分类器和统一的 `page/block` 数据结构，再接入 OCR 和问题矩阵。

