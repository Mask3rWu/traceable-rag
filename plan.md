# 基于国军标和论文的可溯源毁伤评估准则构建计划

## 1. 项目定位

项目主要聚焦于利用国军标和相关论文，构建一套面向视觉证据、来源明确、推导过程可追溯的毁伤评估准则。

RAG 不用于赋予准则权威性，而用于辅助原始资料检索、跨文档综合和证据溯源。现有 YOLO/MLLM 评估流水线作为准则的应用与验证载体。

## 2. 核心流程

### 资料层

国军标与论文 -> 文档解析、OCR、章节和表格切分 -> 可检索原始资料库

### 准则构建层

问题矩阵 -> 逐文档证据抽取 -> 术语与等级标准化 -> 冲突分析 -> 规则综合 -> 可溯源评估准则

### 应用验证层

将准则接入现有 MLLM 毁伤评估流程，通过人工或专家评价、真实与合成数据实验验证其合理性和有效性。

## 3. 准则构建

评估成果应称为“基于标准和相关研究构建的视觉毁伤评估准则”，并区分：

- 直接规则：原文明确规定；
- 综合规则：多个来源归纳得到；
- 研究假设：为适配视觉评估自行定义，需通过实验验证。

### 最小问题矩阵

1. 资料适用于哪些目标和场景？
2. 如何定义毁伤、失效和破坏？
3. 采用哪些毁伤等级？
4. 包含哪些评估维度和关键部件？
5. 各部件有哪些视觉可见毁伤特征？
6. 哪些指标无法仅通过图像确定？
7. 特征如何映射到毁伤等级？
8. 是否存在定量阈值或必要、充分条件？
9. 多部件和多帧结果如何综合？
10. 遮挡、模糊和证据不足时如何处理？
11. 规则有哪些适用条件？
12. 不同来源是否存在冲突？

先选取少量代表性标准和论文试抽取，根据新增信息迭代问题矩阵，稳定后再处理全部资料。

### 证据记录要求

每条结论至少记录：规则编号、目标类型、部件、可见特征、等级关系、原文、文件名、章节、页码、推导类型和置信度。

资料构建应采用“逐文档完整抽取 -> 证据标准化 -> 跨文档综合”，不能仅依赖 Top-K RAG，以免遗漏关键信息。

## 4. 准则应用

现有实时链路保持为：YOLO 跟踪 -> 关键帧选择 -> MLLM 单帧评估，不在逐帧推理中加入 RAG，避免增加延迟。

MLLM 单帧输出应统一包含：目标编号、目标类型、部件、可见特征、等级、置信度、可见度和证据帧，并明确引用所使用的准则编号。

多帧结果的异步聚合、RAG 溯源报告和毁伤知识问答暂作为后续扩展，不进入当前核心实时链路。

## 5. 可信度与验证

- 检查证据覆盖率、引用准确率、规则冲突和综合推导的合理性。
- 明确准则主要覆盖图像可观察的外观毁伤；功能毁伤证据不足时不得强行判断。
- 使用少量独立真实数据或人工盲评验证合成数据，避免生成与评估使用同一规则形成闭环。
- 对比 MLLM 直接评估与加入结构化准则后的评估结果。
- 评价专家一致性、准确率、宏平均 F1、加权 Kappa、引用正确率、幻觉率和拒答能力。

## 6. 实施顺序

1. 建立问题矩阵和统一抽取格式。
2. 试处理代表性资料并修订矩阵。
3. 完成全量资料解析和证据抽取。
4. 开展术语统一、冲突分析和跨文档综合。
5. 形成带来源映射的结构化评估准则。
6. 将准则接入现有 MLLM 流水线进行验证。
7. 开展专家评价、性能测试和对比实验。
8. 将异步汇总和可溯源知识问答作为后续扩展。
